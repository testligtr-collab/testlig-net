framework:
    rate_limiter:
        # Example limiter for later phases (login, API). Not wired to routes yet.
        anonymous_api:
            policy: 'fixed_window'
            limit: 100
            interval: '1 minute'
