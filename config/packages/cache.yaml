framework:
    cache:
        # Filesystem is the safe default without Redis.
        # To use Redis in Docker/dev, set default_redis_provider and switch the app adapter:
        # app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'
