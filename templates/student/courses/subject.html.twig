{% extends 'student/layout.html.twig' %}

{% block title %}{{ subject.name }} — Dersler — Testlig{% endblock %}

{% block student_content %}
<section class="student-catalog">
    <nav class="student-breadcrumb" aria-label="Konum">
        <a href="{{ path('app_student_dashboard') }}">Panel</a>
        <span aria-hidden="true">/</span>
        <a href="{{ path('app_student_courses') }}">Dersler</a>
        <span aria-hidden="true">/</span>
        <span>{{ subject.name }}</span>
    </nav>
    <h1>{{ subject.name }}</h1>
    {% if subject.description %}
        <p class="muted">{{ subject.description }}</p>
    {% endif %}

    {% if units is empty %}
        <div class="empty-state" role="status">
            <p>Bu derste henüz yayımlanmış ünite yok.</p>
        </div>
    {% else %}
        <ul class="student-catalog-grid">
            {% for unit in units %}
                <li>
                    <a class="student-catalog-card" href="{{ path('app_student_course_unit', {subjectSlug: subject.slug, unitSlug: unit.slug}) }}">
                        <span class="student-catalog-card__title">{{ unit.name }}</span>
                        <span class="student-catalog-card__action">Üniteye git</span>
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
</section>
{% endblock %}
