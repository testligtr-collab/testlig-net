<section id="haberler" class="hp-section" aria-labelledby="news-title">
    <div class="container">
        <div class="hp-news">
            <span class="hp-news__symbol" aria-hidden="true">▤</span>
            <div>
                <span class="hp-eyebrow">TESTLİG'DEN</span>
                <h2 id="news-title" class="hp-news__title">{{ view.newsTitle }}</h2>
                <p class="hp-section__lead">{{ view.newsBody }}</p>
                <p class="hp-note" role="status">{{ view.newsStatus }}</p>
            </div>
        </div>
    </div>
</section>
